<HomeScreen>

    MDBoxLayout:
        orientation: "vertical"

        MDTopAppBar:
            title: "Home"
            use_overflow: True
            elevation: 0
            right_action_items:
                [
                ["tools", lambda x: root.manager.set_current("settings"), "Setting", "Setting"]
                ]

        MDBoxLayout:
            orientation: "vertical"
